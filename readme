**** Make sure python is installed on your system first*******


Go inside the project folder, open the terminal there (in that folder), run the following commands one by one:

*****macOS & Ubuntu*****

python -m venv myenv
source myenv/bin/activate
pip install -r requirements.txt
python manage.py runserver

Go to brower and paste this url:

http://127.0.0.1:8000/



*****Windows*****
python -m venv myenv
myenv\Scripts\activate
pip install -r requirements.txt
python manage.py runserver


Go to brower and paste this url:

http://127.0.0.1:8000/

